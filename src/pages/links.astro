---
import Layout from '../layouts/Layout.astro';
import PlatformLinkCard from '../components/PlatformLinkCard.astro';
import { platforms } from '../data/platforms';
import { site } from '../config/site';

const linksBackground = '/visuals/saikusakiyomu.png';
---

<Layout title={`${site.title} | Links`} description="公式リンク一覧と説明">
  <section class="links-hero" style={`background-image: url('${linksBackground}')`}>
    <div class="hero-overlay">
      <p class="eyebrow">Official Links</p>
      <h1>Links</h1>
      <p>
        各プラットフォームの役割と最新導線をまとめています。URLは環境変数 `PUBLIC_*_URL` から取得し、未設定時は「URL未設定」と表示します。
      </p>
    </div>
  </section>

  <section class="cards-stack" aria-label="プラットフォームカード一覧">
    {platforms.map((platform) => (
      <PlatformLinkCard platform={platform} />
    ))}
  </section>
</Layout>

<style>
  .links-hero {
    border-radius: var(--radius-lg);
    min-height: clamp(260px, 42vh, 440px);
    border: 1px solid rgba(191, 219, 244, 0.26);
    overflow: hidden;
    background-size: cover;
    background-position: center;
    box-shadow: 0 14px 36px rgba(0, 0, 0, 0.52), 0 0 22px rgba(137, 234, 255, 0.13);
  }

  .hero-overlay {
    min-height: inherit;
    padding: clamp(1.1rem, 4vw, 2rem);
    display: grid;
    align-content: end;
    gap: 0.75rem;
    background: linear-gradient(to top, rgba(4, 7, 12, 0.9) 20%, rgba(4, 7, 12, 0.35) 70%);
  }

  .eyebrow {
    margin: 0;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #d4e5f7;
    font-size: 0.78rem;
  }

  h1 {
    margin: 0;
    font-size: clamp(2rem, 5vw, 3.2rem);
    letter-spacing: 0.03em;
  }

  p {
    margin: 0;
    max-width: 52rem;
    line-height: 1.8;
    color: #ddedfb;
  }

  .cards-stack {
    margin-top: 1rem;
    display: grid;
    gap: 0.9rem;
  }
</style>
